app = "rhood-agent"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"

[processes]
  app = "npx tsx src/index.ts"

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1
